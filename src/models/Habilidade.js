class Habilidade {
  constructor(nome, descricao, nivel, interesses) {
    this.nome = nome;
    this.descricao = descricao;
    this.nivel = nivel;
    this.interesses = interesses;
  }
}

export default Habilidade;
